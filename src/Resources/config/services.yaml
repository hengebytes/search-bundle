services:
    _defaults:
        autowire: true
        autoconfigure: true

    ATSearchBundle\:
        resource: '../../'
        exclude: '../../{Annotation,DependencyInjection,Resources,Query,Exception,Enum,Search/ValueObject}'
